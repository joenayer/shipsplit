{
  "name": "shipsplit-api",
  "private": true,
  "type": "module",
  "description": "ShipSplit API — Cloudflare Worker + D1 + R2. No build step: wrangler bundles src/index.js directly.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node test/run.mjs && node test/project.mjs"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
